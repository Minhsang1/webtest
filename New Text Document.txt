cài thử viện: pip install flask

Nếu trong thư mục đang có file database.db, hãy xóa nó đi để đảm bảo cấu trúc bảng mới (có thêm cột full_name, email, rating, sold) được khởi tạo chính xác.( lát dùng lệnh python init_db.py sẽ tự tạo cái file này ra mỗi lần chạy lại là m sẽ xóa cái file database.db này xong chạy lại lệnh python init_db.py còn nếu chạy vẫn ổn thì khỏi xóa)

python init_db.py ( tạo database)

python app.py( chạy sever web)

mở web lên và vô link: http://127.0.0.1:5000

Thông tin đăng nhập mặc định:( có thể đăng ký, sau khi đăng ký tự lưu về dtb, dừng chương trình thì sẽ mất phải đăng ký lại, nếu kh đăng nhập hay đky đc thì xóa file dbt ở trên và chạy lại lện python init_db.py(chỉ dùng cho tk tự đky thôi ))

Tài khoản Quản trị (Admin):
User: admin
Pass: admin_P@ssw0rd

Tài khoản Khách hàng (Customer):
User: khachhang1
Pass: 123456

